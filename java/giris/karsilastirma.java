package giris;

public class karsilastirma {
    public static void main(String[] args) {
        int a=10,b=20 ,c=5 ,d;

    boolean kosul1 = (a<b); // false
    boolean kosul2 = (a>c); // false
    boolean sonuc = (kosul1 || kosul2); // false
    d = (a == b) ? 2:3 ; // 3  eğer a==b ise 2 değilse 3
    System.out.println(d);


    
    }
   

// Karşılaştırma Operatörleri



// == Eşittir ( x == y )

// != Eşit Değildir ( x != y )

// > Büyüktür ( x > y )

// < Küçüktür ( x < y )

// >= Büyük Eşittir ( x >= y )

// <= Küçük Eşittir ( x <= y )

// && Ve ( x && y )
// // || Veya ( x || y )


    
}
